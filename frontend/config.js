// config.js — set your Render backend URL here once.
// It can also be changed at runtime from the login screen (saved in the browser).
window.APP_CONFIG = {
  API_BASE_URL: 'https://your-backend.onrender.com',
  APP_NAME: 'AI Call Assistant',
};
